{
  "name": "@runes/matron",
  "version": "0.0.1",
  "main": "dist/matron.js",
  "license": "GPL-3.0",
  "scripts": {
    "configure": "emcmake cmake -DCMAKE_INSTALL_PREFIX=./ -Bbuild -H.",
    "build": "cmake --build build --target matron",
    "build-install": "cmake --build build --target matron install",
    "clean": "rm -rf build/*"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.6"
  }
}
