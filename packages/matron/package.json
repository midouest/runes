{
  "name": "@runes/matron",
  "version": "0.0.1",
  "main": "dist/matron.js",
  "license": "GPL-3.0",
  "scripts": {
    "configure": "emcmake cmake -Bbuild -H.",
    "build": "cmake --build build --target matron",
    "preinstall": "mkdir -p dist && cp build/matron.* dist",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.6"
  }
}
