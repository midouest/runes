{
  "name": "@runes/matron",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "GPL-3.0",
  "scripts": {
    "build": "emcmake cmake -Bbuild -H. && cmake --build build --target index && cp index.d.ts build",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "@types/emscripten": "^1.39.6"
  }
}
