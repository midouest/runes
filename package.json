{
  "name": "@runes/runes",
  "private": true,
  "description": "Norns screen live-coding",
  "license": "GPL-3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "predeploy": "yarn workspace @runes/web build",
    "deploy": "gh-pages -d packages/web/build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
